- defaults:
    link:
        relink: true

- clean: ["~"]

- clean:
    ~/.local/bin:
        force: true
- link:
    #~/.zshrc: zshrc
    ~/.config/:
        glob: true
        path: config/*
    ~/.local/bin/:
        glob: true
        path: bin/*
    ~/.local/share/navi/cheats/kuro_cheats: navi
    ~/.fishrc: config/fish/config.fish

#- shell:
  #- [git submodule update --init --recursive, Installing submodules]
